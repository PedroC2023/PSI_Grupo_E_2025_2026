RewriteEngine On

# Se o ficheiro ou diretório existir, usa-o diretamente
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d

# Caso contrário, envia tudo para o index.php
RewriteRule . index.php [L]
