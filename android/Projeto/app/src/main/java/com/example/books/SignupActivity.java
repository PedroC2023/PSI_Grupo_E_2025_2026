package com.example.books;

import com.example.books.modelo.UserProfile;

public class SignupActivity {
    UserProfile user = new UserProfile(uid, name, email);
// guardar user na BD (Firebase / local)
}
